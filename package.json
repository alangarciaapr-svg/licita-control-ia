{
  "name": "licita-control-ia",
  "version": "0.1.0",
  "private": true,
  "description": "Radar inteligente y auditable para oportunidades de Mercado Público de Chile.",
  "scripts": {
    "check": "npm --prefix worker run check && node --check frontend/app.js && node --check frontend/view-utils.js && node --check frontend/sw.js",
    "test": "npm --prefix worker test && node --test test/frontend.test.mjs",
    "dev:api": "npm --prefix worker run dev",
    "deploy:api": "npm --prefix worker run deploy"
  },
  "engines": {
    "node": ">=22"
  }
}
